# 复制为 .env 后填写真实值（.env 已被 .gitignore 忽略，严禁提交）
# 完整变量说明见 docs/IT_handover.md；非秘密项给出当前离线实验拓扑，密码和密钥保持空白。

# ---- OpenAI 兼容模型接口（当前实验环境为 Windows Ollama）----
# 必须填写 RAG 服务真正能访问到的内网 Ollama 地址。程序在启动时会校验该地址是否属于
# 内网；填公网地址（含历史默认值 https://api.deepseek.com）会被直接拒绝启动。
# 容器内不要写 127.0.0.1（那指向容器自身）；同一 Compose 网络请用服务名，例如 http://ollama:11434/v1
DEEPSEEK_API_KEY=ollama
DEEPSEEK_BASE_URL=
DEEPSEEK_MODEL=qwen3:1.7b
DEEPSEEK_TIMEOUT_S=180
# 模型主机若是受控内网机、但不属于私有网段或常见内网后缀，列在这里（逗号分隔）
RAG_LLM_TRUSTED_HOSTS=
# 生成模型在内存中的驻留时长。默认 5 分钟闲置即卸载，下一次提问要重新加载模型
# （纯 CPU 机器实测约 +25 秒）。设为 30m 可让常用的个人库基本感觉不到冷启动；
# 设为 -1 表示永不卸载（延迟最低，但会长期占用约 2.4 GB 内存，内存紧张的机器不要用）。
# 生效条件：只有「由启动脚本拉起 Ollama」的宿主模式会继承本文件的环境变量。
#   用系统托盘 / systemd 服务方式启动的 Ollama 不读本文件，请改为设置 Ollama
#   自己的环境变量（Windows 用户环境变量，或 Linux 的 unit 文件）。
# 另注意：必须在 Ollama 服务端设置；实测在请求体里传 keep_alive 对 OpenAI 兼容端点
#   （/v1/chat/completions）无效。
OLLAMA_KEEP_ALIVE=30m

# ---- 安全 ----
# 会话令牌 HMAC 密钥：python -c "import secrets;print(secrets.token_urlsafe(48))"
RAG_SECRET_KEY=
# root 初始密码：仅在数据库尚无任何用户时用于创建 root
RAG_ROOT_PASSWORD=
RAG_COOKIE_SECURE=
RAG_SESSION_TTL_HOURS=

# ---- 对外地址 ----
RAG_PUBLIC_ORIGIN=

# ---- 存储目录 ----
RAG_DATA_DIR=
RAG_DB_PATH=
RAG_UPLOAD_DIR=
RAG_MODELS_DIR=

# ---- 嵌入模型 ----
# 离线部署请指向已下载到本机的模型目录（Compose 内为 /rag/models/bge-small-zh-v1.5，
# 宿主模式为 models/bge-small-zh-v1.5）。写成 Hugging Face 仓库名时，只有在 HF 缓存
# 已预热、离线命中成功的情况下才能加载；冷缓存 + HF_HUB_OFFLINE=1 会导致 /api/ready 一直 503。
RAG_EMBED_MODEL=/rag/models/bge-small-zh-v1.5
RAG_EMBED_BACKEND=st
# 嵌入模型运行设备：cpu（默认）| mps | cuda | cuda:<序号>。
# 默认保持 CPU 是刻意选择：实测检索只占 32~530 ms，GPU 编码对"问答"几乎无收益；
# 它的价值在批量入库（上传大文件时），而且会和同机的生成模型争抢显存。
# 显式写成 cuda 却不可用时不会静默退回 CPU——/api/ready 会返回 503 并说明原因，
# 管理页「嵌入设备」也会如实显示，避免"以为在用 GPU"。
RAG_EMBED_DEVICE=cpu
# 批量编码大小；GPU 上可调大以提升入库吞吐（1~256），CPU 上通常无需改动。
RAG_EMBED_BATCH_SIZE=32

# ---- 检索与切块 ----
# 送进生成模型的片段条数。这是纯 CPU 机器上最有效的提速旋钮：实测把 5 降到 3，
# 输入 token 从 1245~1826 降到 802~1087，单次问答从 32~43 秒降到 18~22 秒，答案内容不变。
# 代价是送给模型的上下文更窄，长文档中分散在多处的答案可能不完整。
# 注意：该结论来自 LP 手册上 3 个问题的抽样，正式推广前请用真实评测集重新确认召回率
# （见 eval/README.md）。也可在管理页“运行参数”里在线调整，无需重启。
RAG_TOP_K=3
RAG_MIN_RELEVANCE_SCORE=
RAG_CHUNK_MAX_TOKENS=
RAG_CHUNK_OVERLAP_TOKENS=

# ---- 限流与并发 ----
RAG_QUERIES_PER_MINUTE=
RAG_MAX_CONCURRENT_LLM=
RAG_LLM_MAX_TOKENS=
RAG_LLM_TEMPERATURE=

# ---- 上传 ----
RAG_MAX_UPLOAD_MB=

# ---- 解析资源上限（防压缩炸弹与超大表格）----
# 上传体积上限挡不住"解压后几十 GB"的构造文件：DOCX/XLSX 本身就是 ZIP，
# 几 MB 的文件可以把 word/document.xml 压成几十 GB。下面几项在解压前先做体检。
# 默认值对设备手册这类资料非常宽松，正常文件不会触发；触发时错误信息会说明原因。
# 注意：调大任何一项之前，先确认服务器内存足够——内存索引是常驻的。
# DOCX/XLSX 解压后允许的总大小（MB）
RAG_PARSE_MAX_UNCOMPRESSED_MB=
# 压缩包内允许的条目数
RAG_PARSE_MAX_ZIP_ENTRIES=
# 允许的最大压缩比（解压后 / 压缩后），超过视为压缩炸弹
RAG_PARSE_MAX_COMPRESSION_RATIO=
# 单份文件允许解析出的文本单元数（段落/行/页）
RAG_PARSE_MAX_UNITS=
# 单份文件允许提取的总字符数
RAG_PARSE_MAX_TEXT_CHARS=
# 单份文件允许产生的切片数（直接决定内存索引占用与重建耗时）
RAG_PARSE_MAX_CHUNKS=

# ---- 服务监听 ----
# 注意：容器内这两个变量不影响实际绑定（由 Dockerfile 的 CMD 固定为 0.0.0.0:8088）；
# 只有宿主模式脚本（setup_linux.sh / scripts/start_local.ps1）会用它们拼 uvicorn 参数。
RAG_HOST=
RAG_PORT=

# ---- 启动安全校验（见 app/config.py 的 validate_or_raise）----
# 默认关闭。置 1 会跳过密钥缺失 / 模型地址非内网 / Cookie 与来源不匹配的启动校验，
# 仅可用于本机调试，禁止用于共享或企业环境。
RAG_ALLOW_INSECURE_START=

# ---- Hugging Face 镜像（服务器无法直连 HF 时可配，如 https://hf-mirror.com）----
# HF_ENDPOINT=
