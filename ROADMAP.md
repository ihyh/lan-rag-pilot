# 路线图与发布门槛

[返回首页](README.md)

以 1936a0d 的代码核对为起点。这里区分当前能力、待实现要求和部署验收，不声明某台服务器的实时状态。

## 当前能力

文档解析/规则切片、CPU 嵌入、技术标识符与向量混合检索、设备/文档范围、多轮对话、流式回答、来源/反馈、三级角色、备份和基础检查脚本。当前文档库是统一共享库，不是部门隔离知识库。

## 第一优先级：企业准入阻塞

按用户逐份授权**已实现**：`documents.visibility`（默认 `shared`）+ `document_acl`
（`subject_type`/`subject_id` 通用主体，当前只实现 `user`）。`root` 可把某份文档改为
「仅指定账号可见」；受限文档在**检索、文档列表、原文访问、历史对话**四条链路统一生效，
撤权后旧对话中引用过它的那一轮同步隐藏；管理员例外在审计中留痕。
越权回归见 `tests/document_acl_check.py`（30 项，走真实 HTTP 依赖链）。

**仍未实现按部门/组隔离。** 授权主体已是通用形式，将来加"按组授权"无需改表结构，
但还需要组管理与对应界面。在此之前，可隔离到**账号**粒度，不能按组织单位批量授权。

已覆盖：检索、列表、引用原文、历史问答与单条问答、写入管理（可见范围仅 root 可改）。
未覆盖：**导出**（当前没有导出功能，若将来新增必须一并接入授权）；
以及"缓存"——浏览器端 Service Worker 已不再缓存 HTML 与 API 响应（见 `tests/sw_cache_check.js`）。

## 第二优先级：程序与部署加固

- ✅ 程序校验本地模型地址、离线要求和随机密钥，关键配置缺失时**拒绝启动**；不再只依赖文档提醒。校验项与逃生开关见 [README](README.md#启动安全校验)，行为测试见 `tests/config_guard_check.py`。
- ✅ 仓库自带 Compose 只把 8088 发布到回环地址；一键脚本在启动后轮询 `/api/health` 与 `/api/ready` 做真实验收（宿主模式），不再只打印“setup completed”。
- ✅ 修正两条会“坏掉也能通过”的断言：限流用例改为严格序列（前 N 次必须 200、第 N+1 次必须 429）并验证用户隔离；重启持久化用例改为比对切片/向量/运行参数基线并真正发起一次问答。重启阶段已接入 CI。
- 评估完整 CSRF 防护、文件解析隔离/资源配额、非 root 容器和依赖更新。
- 将经过目标验收的离线部署/备份流程固化；联网一键脚本不等于经过目标验收的离线安装器。
- 高保证身份、审计、保留/销毁策略按真实企业需求设计，不宣称已有 MFA/SSO。

网络默认拒绝出口、HTTPS、存储与备份保护必须由 IT 落实，程序加固不能替代它们。

## 第三优先级：效果与容量

使用人工标准答案评价检索命中、引用定位、拒答和答案忠实度；记录延迟/资源。比较 BGE 同系列与本地生成模型，每次只改变一项。先证明收益再引入新模型/重排组件，见 [模型指南](docs/MODELS.md)。

## 发布检查

每次发布记录源码 commit、依赖/镜像/模型摘要、离线冷安装、恢复演练、权限与真实问题验收。代码合并、软件包交付和目标部署是不同状态。

严格离线范围不包含云 API、企业微信/飞书云入口、在线更新。历史计划与实验数据只保留在 Git 版本历史中，不能作为现在已完成的证据。
