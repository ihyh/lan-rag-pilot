# 局域网 RAG 后续完善路线图

> 原则：先稳定与安全，再用评测驱动检索优化；达到规模门槛后才更换数据库和架构。

## 当前基线（2026-09-13）

- 架构为 FastAPI + SQLite + 本地 BGE 检索 + 内网 Ollama 生成；不允许自动回退到 DeepSeek 公网服务。
- 已实现统一共享文档库、root/kb_admin/user 权限、PDF/DOC/DOCX/XLSX/TXT/MD 批量上传、引用、反馈、审计、限流和并发控制。
- 已实现多轮对话、设备/文档范围、旧问答迁移与兼容接口；检索增加精确技术词补召回和近重复去除，Qwen3 请求关闭隐藏思考。
- 2026-09-13 本地 `codex/refresh-project-docs` 功能分支已实现设备版本范围匹配、管理弹窗修复、角色权限展示和浏览器流式回答；完整隔离 smoke 149/149、专项回归和重启持久化通过。尚无可据以宣布真实问答准确率达标的固定人工评测报告。
- 当前 Ubuntu VM 已部署流式回答提交 `71d35bb`；部署后容器 healthy、重启次数为 0，SQLite 完整且数据数量不变，BGE 就绪，Ollama `qwen3:1.7b` 用合成文本完成流式验证。尚未用真实业务文档做端到端浏览器验收。
- 发布状态：2026-09-13 检查时 GitHub `main` 为 `d0680f0`，包含此前 PR #4/#5/#6；上述四项新增代码仅位于本地 `codex/refresh-project-docs` 功能分支，尚未进入 GitHub。功能分支、PR、主分支合并和 VM 部署分别验收。
- 运维待验收项：稳定地址、HTTPS、独立账号与强口令、访问控制、自动备份和恢复演练；不能把模板存在等同于目标环境已完成。

## P0：团队试点前必做

1. ✅ 当前 Ubuntu VM 已升级到应用提交 `71d35bb`，并完成容器、数据库、模型、入口及流式模型验证；公司目标机仍需单独部署验收。
2. 为宿主机设置 DHCP 保留和内部 DNS，不再使用随网络变化的个人电脑 IP；2026-09-09 已实际发生 `172.16.3.50` 漂移到 `172.16.3.56`。
3. ✅ 已提供 Nginx HTTPS 与 Compose 本机绑定模板；证书、DNS 和反向代理仍需在目标机执行，并设置 `RAG_COOKIE_SECURE=true`。
4. 确认已替换所有示例口令及开发密钥，每位成员使用独立账号，禁止共用 root；不在仓库记录实际凭据。
5. 限制防火墙：只允许公司网段访问 443，管理入口不开放公网。
6. ◐ 已提供每日备份 cron 安装脚本和恢复检查脚本；安装状态、最近成功备份及“新机器恢复”结果需在目标环境实时核验。
7. ✅ 已提供 liveness `/api/health` 和 readiness `/api/ready`，Compose healthcheck 使用 readiness；监控接入仍需在公司环境完成。

验收标准：重启虚拟机后服务自动恢复；内部域名可访问；全程 HTTPS；备份可恢复；连续运行 7 天无需人工重新启动。

## P1：建立真实评测，改善问答质量

1. 从现有手册整理 30～50 个真实问题，并记录标准答案、正确页码和应命中的段落。
2. 分开统计：检索是否命中、引用是否正确、答案是否忠于原文、无答案时是否拒答。
3. 将已出现的拒答/漏召回样本纳入评测，检查正确原文是否入库、目标段落是否命中，以及近重复片段是否挤占 Top-K；用结果决定是否调整切块、重叠和去重。
4. ✅ 增加 `RAG_MIN_RELEVANCE_SCORE` 低相似度拒答阈值（默认 0.25，需用真实评测集调参）。
5. ✅ 已增加面向型号、参数名和命令名的 SQLite 正文精确词补召回，与 BGE 向量结果合并排序；当前不是完整 FTS5/BM25。
6. 真实评测若仍证明召回或排序不足，再评估 FTS5/BM25 或轻量 reranker；不要仅凭个别问题增加第二个模型。

建议验收门槛（项目目标，不是当前实测）：固定评测集 Top-5 检索命中率 ≥ 90%，引用位置正确率 ≥ 95%，无依据问题不编造答案。

## P2：团队使用体验与权限

1. ✅ 2026-09-05 按用户要求取消部门/知识库划分，改为全部登录用户共享统一文档库。
2. ✅ 保留系统管理员（root）、文档管理员（kb_admin）和只读用户（user）；文档管理员可维护全部文档，个人问答仍隔离。
3. ✅ 上传只需文件和可选版本，系统记录上传人和上传日期；可按版本/上传日期筛选。已移除标签、生效日期、部门和知识库填写入口，旧列仅保留兼容数据。
4. ◐ 已有前端批量上传、逐文件状态和失败后重建索引；真实长耗时场景仍需评估是否引入任务队列。
5. ✅ 增加答案“有帮助/无帮助”和问题反馈，供评测集持续补充（首版支持 root 查看）。
6. ✅ 引用来源默认折叠，按正文引用编号展示 PDF 页码与原文摘录；登录后可打开全部 ready 原文，PDF 链接附带页码锚点。
7. ✅ 多轮对话：最近 3 个成功轮次、最多 2000 字历史辅助追问，检索结合上一问题和当前问题；来源仅对应本轮。普通用户只管理自己的对话，root 可查看/删除任意对话。
8. ✅ 旧问答自动迁移为单轮对话，保留旧 `/api/chats` 接口；删除对话级联清理问答、来源和反馈。mock 回归通过不替代真实部署验收。
9. ✅ Qwen3 关闭隐藏思考、回答规范化、设备快捷范围、文档范围持久化和流式回答已部署到当前 Ubuntu VM；仍需用固定真实样本持续测冷启动、热模型、并发耗时和忠实度。GPU 服务器部署尚未实施。

验收标准：启用的登录账号可检索全部文档；普通用户不能管理文档或查看他人问答；文档管理员不能管理用户；root 可追踪文档版本、失败任务和反馈。

## P3：企业接入与数据治理

1. 第一阶段继续使用企业微信/飞书工作台网页链接，先验证实际使用率。
2. 确认确有免密需求后，再接企业身份 SSO；不要同时开发两个平台的机器人。
3. 只有用户确实需要在群聊中提问时，再开发一个平台的机器人试点。
4. 保持离线数据边界：只向批准的内网模型发送当前问题、检索片段和有界历史，不向公网模型自动回退；保留用户、时间、模型、token 用量和来源审计。
5. 明确获准入库的资料范围，对敏感字段增加上传前检查或脱敏；生产原文、数据库、凭据、模型缓存和本地工作记录不进入 GitHub。
6. 制定审计和问答历史保留周期，支持管理员按策略清理。

验收标准：账号离职后能统一禁用；模型调用和引用来源可审计；敏感资料及模型使用范围得到公司安全/法务确认。

## P4：达到门槛后再扩容

满足任一条件再启动架构迁移：切片接近 5 万、并发排队明显、需要多副本高可用、SQLite 写入成为瓶颈。

届时按顺序迁移：

1. 增加数据库迁移工具并建立版本化 schema。
2. SQLite 迁移到 PostgreSQL + pgvector。
3. 上传原文件迁移到公司文件存储或对象存储。
4. 文档解析和向量化迁移到后台任务队列。
5. API 再改为多 worker/多副本部署。

## 暂时不要做

- 不上 Kubernetes、微服务或独立向量数据库。
- 不引入 LangChain，当前直接调用链已经足够清楚。
- 不做 Windows EXE；浏览器入口更适合团队更新和权限控制。
- XLSX 文本解析已支持；暂不增加 OCR、旧版 XLS、PPT 或双平台机器人，除非真实试点反馈证明有需求。
- 不为“可能增长”提前重写系统，以实际容量和评测数据作为升级依据。

## 推荐的下一轮工作

1. 先完成 DHCP 保留或迁移到有固定地址的服务器，再配置内部 DNS，避免员工入口随重启变化。
2. 启用 HTTPS、`RAG_COOKIE_SECURE=true`、强口令和强会话密钥，并从另一台局域网设备验收。
3. 建立第一版至少 30 个问题的真实评测集，记录检索、引用、拒答、回答质量与耗时基线；数据及报告保留在受控环境。
4. 根据评测基线只修仍存在的漏召回和错误排序；不要用更大生成模型替代检索问题。
5. 在目标 GPU 服务器确定后制作离线镜像/模型包，迁移前备份并做恢复检查，部署后重新测固定问题集。
6. 每次发布继续走功能分支 → PR → CI → 备份 → 部署 → 实时健康/数据/模型验收，不直接推送 `main`。
